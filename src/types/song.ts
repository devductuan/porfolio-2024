export type SongType = {
    id: string,
    title: string,
    author: string,
    link: string,
    image: {
        src: string,
        width: number,
        height: number,
        alt: string,
    }
}